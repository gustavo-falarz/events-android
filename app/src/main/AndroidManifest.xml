<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pinecone.events">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".EventsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.splash.SplashView"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.newEvent.NewEventView"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.eventDetails.EventDetailsView"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.signUp.SignUpView"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.signin.SignInView"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.events.EventsView"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>